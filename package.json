{
  "type": "module",
  "dependencies": {
    "pako": "^2.1.0",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^6.0.1"
  },
  "scripts": {
    "deploy": "sudo ntl build && sudo ntl deploy --prod"
}

}
